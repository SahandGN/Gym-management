<form method="post" action="{{ path('app_locker_delete', {'id': locker.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ locker.id) }}">
    <button class="btn">Delete</button>
</form>
