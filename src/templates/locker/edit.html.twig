{% extends 'base.html.twig' %}

{% block title %}{{ 'global.edit' | trans }}{% endblock %}

{% block body %}
    <h1>{{ 'global.edit' | trans }}</h1>

    {{ include('locker/_form.html.twig', {'button_label': 'Update'}) }}

    <a type="button" class="btn btn-outline-warning" href="{{ path('app_locker_index')  }}">{{ 'global.back' | trans }}</a>

    {{ include('locker/_delete_form.html.twig') }}
{% endblock %}
