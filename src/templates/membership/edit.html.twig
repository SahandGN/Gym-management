{% extends 'base.html.twig' %}

{% block title %}{{ 'global.membership' | trans }}{% endblock %}

{% block body %}
    <h1>{{ 'global.membership' | trans }}</h1>

    {{ include('membership/_form.html.twig', {'button_label': 'Update'}) }}

    <a type="button" class="btn btn-outline-warning" href="{{ path('app_membership_index') }}">{{ 'global.back' | trans }}</a>

    {{ include('membership/_delete_form.html.twig') }}
{% endblock %}
